Modernizr.load({
    test: Modernizr.mq('only all'),
    yep: '',
    nope: 'vendor/respond.js'
});

Modernizr.load({
    test: Modernizr.mq('only all'),
    yep: '',
    nope: 'vendor/html5-polyfill.js'
});